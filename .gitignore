__pycache__/
*.pyc
*.log
.DS_Store
ip2asn-v4.tsv
ip2asn.db
chat.json
build/

# C build artifacts
.obj/

# Rust build artifacts
syn-sight/target/

# Compiled binaries
*.bin
tcp_syn_stop

# Apt repository staging (rebuilt by make deb, published to GCS)
repo/

# Stray .deb files
*.deb

# Release script advisory lock
.release.lock

# Release env (contains bucket name, maintainer — never commit)
release.env

# Debian build artifacts (generated by dpkg-buildpackage)
compile_commands.json
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/tcp-syn-stop/
debian/*.substvars
debian/*.postrm.debhelper

# Editor/IDE caches
.cache/

# Private materials (review submissions, reports, plans)
submissions/
*-report*.md
*-report*.txt
plan_bpf.md
